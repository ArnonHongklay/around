<h1><%= t(".header") %> "<%= @post.title %>"</h1>
<%= blog_admin_form_for ["admin", @post], url: blog_admin_posts_path(@post), method: :put do |post| %>
  <%= render partial: 'form', locals: {post: post} %>
<% end %>
