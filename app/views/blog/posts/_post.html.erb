<section data-tags="<%= post.tags.map(&:name).join(' ') %>">
  <%= render partial: 'blog/posts/post_header', locals: {post: post} %>

  <div class="content" data-blog="content">
    <%= truncate_html(post.content, length: Rails.application.config.preview_size, omission: '...') %>
  </div>

  <div class="tags" data-blog="tags">
    <%= render post.tags %>
  </div>
  <%= link_to t(".readmore"), post.full_url, class: "button" %>
</section>
